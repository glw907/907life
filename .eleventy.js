module.exports = function(eleventyConfig) {
  return {
    dir: {
      input: "src",   // Source files folder
      output: "docs"  // Output files to the /docs folder for GitHub Pages
    }
  };
};
